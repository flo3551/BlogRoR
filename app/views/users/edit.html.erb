<% if @user.errors.any? %>
  <div class="alert alert-danger">
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="card mt-5 panel-default">
  <div class="card-header">Modifier mon compte</div>
  <div class="card-body">
    <%= form_for @user, url: profil_path, html: {class: 'form-horizontal'} do |f| %>

      <div class="form-group">
        <%= f.label :username, "Nom d'utilisateur", class: 'col-md-4 control-label' %>
        <div class="col-md-8">
          <%= f.text_field :username, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :email, "Email", class: 'col-md-4 control-label' %>
        <div class="col-md-8">
          <%= f.email_field :email, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :firstname, "Prénom", class: 'col-md-4 control-label' %>
        <div class="col-md-8">
          <%= f.text_field :firstname, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :lastname, "Nom", class: 'col-md-4 control-label' %>
        <div class="col-md-8">
          <%= f.text_field :lastname, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <div class="col-md-offset-4 col-md-6">
          <button type="submit" class="btn btn-primary">
            Modifier mon compte
          </button>
        </div>
      </div>


    <% end %>
  </div>
</div>